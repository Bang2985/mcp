pr:
  branches:
    include:
      - main
      - feature/*
      - hotfix/*
      - release/*
      - users/pahallis/dont-test-skips

trigger: none

extends:
  template: /eng/pipelines/templates/common.yml
  parameters:
    RunLiveTests: ${{ eq(variables['System.TeamProject'], 'internal') }}
